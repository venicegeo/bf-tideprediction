from flask import Flask
import os
from app import app
from waitress import serve
port = int(os.getenv("PORT"))
#app = Flask(__name__)
if __name__ == '__main__':
    #app.run(debug=True)
    serve(app, port=port)

